#include <iostream>

int main() {
    int count = 1;
    while (count <= 10) {
        std::cout << count << std::endl; // Mencetak nilai count
        ++count; // Menambahkan count dengan 1
    }
    return 0;
}
